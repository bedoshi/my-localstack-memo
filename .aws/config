if (!(Test-Path -Path "~/.aws")) {
    New-Item -ItemType Directory -Path "~/.aws"
}

@"
[default]
region = us-east-1

[profile localstack]
region = us-east-1
"@ | Out-File -FilePath "~/.aws/config" -Encoding utf8
